# Copy this file to .env and fill in values as needed.
# Parts 1 and 2 (scraping + cleaning) do not require any API keys.
# These are placeholders in case a key is needed later (Clay handles enrichment).

# Example only — leave blank unless a script actually needs it.
# CRUNCHBASE_API_KEY=
